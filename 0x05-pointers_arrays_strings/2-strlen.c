#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	char *s = string;

	putchar("The length of the string is %d", strlen(str));
	return (string);
}
